import { PerkStyleSelectionDto } from './perk-style-selection-dto';

describe('PerkStyleSelectionDto', () => {
  it('should create an instance', () => {
    expect(new PerkStyleSelectionDto()).toBeTruthy();
  });
});
