pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'ls -al'
        sh 'docker compose build'
      }
    }
    stage('Deploy') {
      steps {
        sh 'docker compose down && docker container prune && docker compose up -d'
      }
    }
  }
}